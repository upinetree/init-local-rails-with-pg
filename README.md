# Usage

```console
$ rails new yourapp -d postgresql
$ cd yourapp
$ curl https://raw.githubusercontent.com/upinetree/init-local-rails-with-pg/master/bin/apply.sh | sh
$ docker-compose up -d
$ bin/rails db:create
$ bin/rails s
```
